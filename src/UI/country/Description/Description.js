import React from 'react'
import { connect } from 'react-redux';

const Description = () => {
  return (
    <div>
      Description
    </div>
  )
}

export default connect(null, null)(Description);
